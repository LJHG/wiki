on: push

jobs:
  publish_site:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: npm install gitbook-cli@2.1.2 --global
    - run: gitbook -V
    - run: pwd
    - run: ls -a
    - run: ./publish.sh
