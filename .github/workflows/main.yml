on: push

jobs:
  publish_site:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: nvm install 10.14.1
    - run: nvm use 10.14.1
    - run: npm install -g gitbook-cli
    - run: gitbook -V
    - run: pwd
    - run: ls -a
    - run: ./publish.sh
